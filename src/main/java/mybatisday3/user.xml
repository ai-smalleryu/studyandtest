<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatisday3.UserMapper">
    <resultMap id="user" type="mybatisday3.User">
        <id property="id" column="teacher_id"/>
        <result property="name" column="name"/>
        <result property="password" column="pasword"/>
    </resultMap>
    <!--    void selectUserId(int id);
    List<User> getUserlikename(String str);
    User getUserById(int id);
    User getUserByMap(Map<String ,Object> map);-->
    <select id="selectUserId" resultMap="user">
        select  teacher_id ,name,pasword from teacher where teacher_id =#{id}
    </select>
    <select id="getUserlikename" resultMap="user">
        select  teacher_id ,name,pasword from teacher where name like #{name}
    </select>
    <select id="getUserByMap" resultMap="user">
        select  teacher_id ,name,pasword from teacher where teacher_id=#{id} or name=#{name}
    </select>
    <delete id="deleteId" >
        delete from teacher where teacher_id =#{id}
    </delete>
</mapper>
